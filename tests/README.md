# Tests

Structural tests for this kit are run via the governance foundation's structural validation tool:

```bash
aieos-governance-foundation/tests/check-structure.sh ./aieos-solution-sourcing-kit
```

This checks file existence, naming conventions, directory structure, and four-file completeness against the standard defined in `aieos-governance-foundation/docs/kit-structure-standard.md`.
